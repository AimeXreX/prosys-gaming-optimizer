# Contributing

Build Release and run the test executable before submitting a change. Keep the dependency direction described in `ARCHITECTURE.md`. New mutation features require detection, compatibility, backup, apply, verification, rollback, rollback verification, structured logging and status documentation. Never add arbitrary command execution, blind service disabling, blanket registry packs or guaranteed performance claims.
